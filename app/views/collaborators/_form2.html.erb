<%= form_with(model: collaborator) do |form| %>
  <% if collaborator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collaborator.errors.count, "error") %> prohibited this collaborator from being saved:</h2>

      <ul>
        <% collaborator.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :telefono %>
    <%= form.number_field :phone_number %>
  </div>

  <div class="field">
    <%= form.label :jefe_directo %>
    <div class="mb-3 "> 
      <%= form.collection_select :admin_id, Admin.order(:name), :id, :name %>
    </div>
  <div>

  <div class="field">
    <%= form.label :porcentaje_de_ganancia %>
    <%= form.text_field :percentage_profit %>
  </div>

  <div class="field">
    <%= form.label :monto_por_servicio %>
    <%= form.text_field :profit_amount %>
  </div>

  <div class="actions">
    <%= form.submit "Crear Profesional" %>
  </div>
<% end %>
